[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
healthCheckPath = "/health"

[[services]]
name = "web"
type = "web"
port = 8080
protocol = "http"